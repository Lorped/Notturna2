import { NonecrotaumPipe } from './nonecrotaum.pipe';

describe('NonecrotaumPipe', () => {
  it('create an instance', () => {
    const pipe = new NonecrotaumPipe();
    expect(pipe).toBeTruthy();
  });
});
